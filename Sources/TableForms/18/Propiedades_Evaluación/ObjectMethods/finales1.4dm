AL_ExitCell (xALP_CsdList2)

vbRecalcPromedios:=True:C214
vi_ConsolidaExamenFinal:=bConsolidaExamenFinal

  // Ticket 175179
  //If (Self->=1)
  //APPEND TO ARRAY(atSTR_EventLog;"Atributo \"Consolidar exámenes finales \" activado")
  //Else 
  //APPEND TO ARRAY(atSTR_EventLog;"Atributo \"Consolidar exámenes finales \" inactivado")
  //End if 